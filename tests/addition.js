function greet(greeting, name) {
//  return greeting + name;
  return greeting + ' ' + name;
}

function format(x) {
//////  return x;
////  return 'h' + '3' + 'l' + 'l' + 'o';
//  return x.replace('e', '3');
  return x.replace('e', '3') + '!';
}
